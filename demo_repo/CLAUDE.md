# CLAUDE.md — demo_repo

Dette er et lille eksempel-repository med en `calculator`-pakke.

- Kildekode ligger i `src/`.
- Tests ligger i `tests/` og køres med pytest.
- Ret kun den funktion, en opgave konkret beder om — undgå at ændre andre funktioner.
